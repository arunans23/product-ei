language: java
install: true
jdk:
  - oraclejdk8
script: mvn clean install -fae -B -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn

cache:
  directories:
    - $HOME/.m2